package fm.juke.mobile.core.design

import androidx.compose.ui.graphics.Color

/**
 * Shared palette lifted from the SwiftUI design system so Compose can stay visually in-sync.
 */
object JukePalette {
    val Background = Color(0xFF030712)
    val Panel = Color(0xFF090F1F)
    val PanelAlt = Color(0xFF0F172A)
    val Accent = Color(0xFFF97316)
    val AccentSoft = Color(0xFFFB923C)
    val AccentDark = Color(0xFFEA580C)
    val Text = Color(0xFFE2E8F0)
    val Muted = Color(0xFF94A3B8)
    val Border = Color(0x14FFFFFF)
    val Success = Color(0xFF16A34A)
    val Warning = Color(0xFFFACC15)
    val Error = Color(0xFFEF4444)
    val IndigoGlow = Color(0xFF1E3A8A)
}
